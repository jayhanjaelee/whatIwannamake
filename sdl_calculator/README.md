# calculator

This is a calculator GUI application written in C.

## requirements

`sdl2`
`sdl2_ttf`

`brew install sdl2`
`brew install sdl2_ttf`
`brew install pkg-config`

## build

`./build.sh`

## run

`./calc`
